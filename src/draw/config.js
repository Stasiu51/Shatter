const pitch = 50;
const rad = 10;
const OFFSET_X = -pitch + Math.floor(pitch / 4) + 0.5;
const OFFSET_Y = -pitch + Math.floor(pitch / 4) + 0.5;
const TIMELINE_OFFSET_X = 40;

export {pitch, rad, OFFSET_X, OFFSET_Y, TIMELINE_OFFSET_X};
